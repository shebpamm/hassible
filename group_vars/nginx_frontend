---

nginx_upstream_ip: 127.0.0.1

nginx_revproxy_sites:
  letsencrypt_email: changethis@example.com

  default:
    ssl: false
    letsencrypt: false

  hass.example.com:
    domains:
      - hass.example.com
    upstreams:
      - { backend_address: "{{ nginx_upstream_ip }}", backend_port: 8123 }
    ssl: true
    letsencrypt: true
