---

_install_dir: /opt/docker
_instance_name: homestack-debug


homestack_config:
  timezone: Europe/Helsinki


  install_dir: "{{ _install_dir }}"
  instance_name: "{{ _instance_name }}"

  install_source: "{{ _install_dir }}/{{ _instance_name }}"
